import styled from 'styled-components';

const Button = styled.button`
  fill: grey
  height: 50px;
  width: 50px;

  background: none;
  border: none;
  &:hover {
    fill: black;
  }
`;

export default Button;
