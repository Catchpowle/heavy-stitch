import styled from 'styled-components';
import button from './button';
import { ButtonBack } from 'pure-react-carousel';

const BackButton = styled(ButtonBack)`${button}`;

export default BackButton;
