import styled from 'styled-components';
import button from './button';
import { ButtonNext } from 'pure-react-carousel';

const NextButton = styled(ButtonNext)`${button}`;

export default NextButton;
